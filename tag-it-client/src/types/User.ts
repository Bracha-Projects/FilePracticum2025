export interface User {
    id: number
    firstName: string
    lastName: string
    password: string
    email: string
    role: string // Assuming RoleType is a string; replace with the exact type if necessary
  }